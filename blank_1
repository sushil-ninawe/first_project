
import datetime
import pytz

# Define the time and days for the trigger
trigger_time = datetime.time(22, 0)  # 22:00 EST
trigger_days = [1, 2, 3, 4, 5]  # Tuesday, Wednesday, Thursday, Friday, Saturday (0=Monday, 1=Tuesday, etc.)

# Get the current time in the EST time zone
est = pytz.timezone('US/Eastern')
current_time = datetime.datetime.now(est).time()
current_day = datetime.datetime.now(est).weekday()  # 0=Monday, 1=Tuesday, etc.

# Check if the current day and time match the trigger conditions
if current_day in trigger_days and current_time == trigger_time:
    # Trigger the scenario
    print("Triggering scenario...")
    # Add your code here to trigger the Dataiku scenario

# If you want to trigger the scenario, you'll need to call the Dataiku API to trigger it programmatically. 
# You can use the Dataiku Python API to do this.
